completed:
db support for mysql and sqlite added.
logging using loguru added for both console and file output.
logging middleware added.
add request log tracking to database.
added logging the ip, client key, user-agent to db.
added exception classes.
added basic authentication using keys parsed from yaml file.
added custom router url prefix support


pending:
configure jwt.
configure endpoints for functionality.
configure exception handlers for entire app.
create endpoints for getting log count from api keys.
configure to fetch the request queries from the db (requestLogs table) according to the key inputted.

#
configure the status endpoint, model task updation is working now